<%= form_for [:admin, @shape] do |form| %>
	<ul>
		<% @shape.errors.full_messages.each do |error| %>
			<li><%= error %></li>
		<% end %>
	</ul>

	<%= form.label :name %>
	<%= form.text_field :name %><br>
	
	<%= form.submit %>
<% end %>